'use strict';

var healthModule = angular.module('healthModule.services', []);


//	Service Name : landingPage which includes all functions based on its module.
healthModule.service('healthModuleService', function ($http, $timeout, ENV, $rootScope, $cookieStore) {
   var servicePath = ENV.apiEndpoint;              // Base path of API
   var URL;
  // --------------------------------------START EVENTS AREA ---------------------------------------------
    // Function Name : getGatewayList
    
    
    // --------------------------------------END EVENTS AREA ---------------------------------------------
	
});