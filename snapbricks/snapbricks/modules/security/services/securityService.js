'use strict';

var securityModule = angular.module('securityModule.services', []);


//	Service Name : landingPage which includes all functions based on its module.
securityModule.service('securityModuleService', function ($http, $timeout, ENV, $rootScope, $cookieStore) {
   var servicePath = ENV.apiEndpoint;              // Base path of API
   var URL;
  // --------------------------------------START EVENTS AREA ---------------------------------------------
    // Function Name : getGatewayList
    
    
    // --------------------------------------END EVENTS AREA ---------------------------------------------
	
});