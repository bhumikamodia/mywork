'use strict';

var dataModule = angular.module('dataModule.services', []);


//	Service Name : landingPage which includes all functions based on its module.
dataModule.service('dataModuleService', function ($http, $timeout, ENV, $rootScope, $cookieStore) {
   var servicePath = ENV.apiEndpoint;              // Base path of API
   var URL;
  // --------------------------------------START EVENTS AREA ---------------------------------------------
    // Function Name : getGatewayList
    
    
    // --------------------------------------END EVENTS AREA ---------------------------------------------
	
});